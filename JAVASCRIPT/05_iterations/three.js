// for of

// for (const element of object) {
    
// }
const arr = [1,2,3,4,5]

//can be applied on any strings,objects
for (const i of object) { 
    console.log(i);
}

const greetings = "Hello"
for (const greet of object) {
    console.log("Each char is " + greet);
    
}